# Edit Profile

Screen:
- lib/screens/edit_profile_screen.dart

Named Route:
- EditProfileRoutes.editProfile (`/profile/edit`)

Ways to open:
- From Profile header "Edit" button
- From Profile > Settings > "Edit Profile"
- By route:
  Navigator.of(context).pushNamed(EditProfileRoutes.editProfile);

Notes:
- This is a placeholder UI (stub) for preview with Ocean Professional theme.
- Includes fields for name and email with a Save action (stubbed SnackBar).
