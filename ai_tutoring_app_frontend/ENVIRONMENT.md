# Environment Variables

This Flutter preview app does not require environment variables to build or analyze.

- The pubspec.yaml includes `.env` under assets for future use, but it is optional.
- You can create an empty `.env` file if your CI requires its presence.

Optional:
- touch .env
