# Assets

This directory contains images and other static files for the AI Tutoring App preview.

- You can place PNG/JPEG/SVG files here and reference them in code.
- The pubspec.yaml is already configured to include the `assets/` directory.

Note: For the current minimal scaffold, there are no required assets.
