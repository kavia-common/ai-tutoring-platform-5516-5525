AI Tutoring Platform Workspace

Flutter app location:
- ai_tutoring_app_frontend

Run commands from the Flutter app directory:
  cd ai_tutoring_app_frontend
  flutter pub get
  flutter analyze
  flutter test
  flutter run

CI hint:
- Some CI jobs auto-detect the project root using marker files. This repo includes FLUTTER_PROJECT_PATH.txt to point to the app directory.
