This Flutter app currently targets Android/emulator in CI. The iOS directory is intentionally minimal here.
If you build for iOS locally, run:
  flutter create .
from this directory to scaffold iOS project files, or open Xcode after enabling iOS support.
