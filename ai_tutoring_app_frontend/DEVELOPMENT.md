# Development Notes

Run Flutter commands from this directory:

- flutter pub get
- flutter analyze
- flutter test
- flutter run

If you are running from the repository root, use the helper:
- bash ../../run_flutter_checks.sh
or the root wrapper:
- ../../flutterw analyze
- ../../flutterw test
